# Release Notes

## 🐛 Bug 修复

### 缓存和下载播放修复
- **修复断网无法播放已缓存歌曲**：重构 `PlaybackService` 数据源链路，将缓存检查提前到网络取址之前
- **修复已下载歌曲无法播放**：使用 `DefaultDataSource` 支持 `content://`、`file://` 和旧版本裸路径
- **修复音质切换混用缓存**：缓存 key 现在包含音质信息，避免不同音质相互干扰
- **修复失效下载阻断播放**：失效的本地文件现在会自动回退到缓存或在线播放
- **支持缓存开关**：设置中的"歌曲缓存"开关现在真正生效

### 登录状态持久化修复
- **修复退出重进后登录失效**：`CookieStore` 现在确保在 `PlayerViewModel` 初始化前完成数据加载
- **优化初始化时序**：添加 `awaitReady()` 机制，避免竞态条件
- **保持登录凭据**：网络错误不再清除已保存的登录 token

## ✨ 功能改进

### 全屏播放器重构
- **默认显示唱片**：打开全屏播放器时默认显示旋转的音乐唱片
- **点击切换歌词**：点击唱片可切换到歌词视图，点击歌词返回唱片
- **网易云式体验**：模仿网易云音乐的交互方式
- **保留完整功能**：歌词滚动、翻译、逐字高亮等功能完整保留
- **无障碍支持**：TalkBack 语义提示"显示歌词"/"显示唱片"

### 设置页重组
将设置项重新分类为五大功能区块，更易查找：

1. **个性化设置**
   - 全局背景和壁纸
   - 全屏播放器背景
   - 控件透明度

2. **账户信息**
   - 网易云 Cookie 管理
   - 登录状态说明

3. **播放与歌词**
   - 音质选择
   - 悬浮歌词设置

4. **下载与存储**
   - 下载目录管理
   - 缓存管理

5. **后台与高级选项**
   - 电池优化豁免
   - 系统权限说明

## 🔧 技术改进

- 重构数据源链路：`DefaultDataSource` → `CacheDataSource` → `ResolvingDataSource`
- 优化缓存判断：使用 Media3 content metadata 判断缓存完整性
- 改进 URI 处理：统一规范化本地文件路径
- 优化初始化时序：确保数据加载完成后再执行依赖操作

## 📋 测试建议

### 离线播放测试
1. 在线播放一首歌曲
2. 完全断网
3. 确认该歌曲仍可播放（缓存命中）

### 下载播放测试
1. 下载几首歌曲
2. 断网后从"下载管理"播放
3. 确认本地文件正常播放

### 登录状态测试
1. 登录账号
2. 完全退出应用（杀进程）
3. 重新打开应用
4. 确认仍然保持登录状态

### 播放器交互测试
1. 打开全屏播放器（默认显示唱片）
2. 点击唱片切换到歌词
3. 点击歌词返回唱片
4. 确认播放控制正常

### 设置页测试
1. 浏览五个设置分类
2. 修改各项设置
3. 退出重进确认设置保存

## ⚠️ 已知限制

- 缓存目录位于 `app.cacheDir/music`，系统可能自动清理
- 下载文件位于用户选择的目录，为永久存储
- 全屏播放器的唱片/歌词状态不持久化，每次打开默认显示唱片
- 部分缓存的歌曲在断网时可能提示网络不可用（符合预期）

## 📦 安装说明

1. 下载 `app-debug.apk`
2. 如果已安装旧版本，直接覆盖安装即可
3. 首次启动会自动迁移已有设置和登录状态
4. 下载索引和缓存数据会自动兼容

---

**注意**：这是一个 Pre-release 测试版本，建议在测试环境中验证后再正式使用。
